// import { ONLOAD } from "./types";
const initialState = {}

// path: store.auth
function AuthReducer(state=initialState, action, store){

  return state;
}
AuthReducer.eventName = [];
AuthReducer.initialState = initialState;

export default AuthReducer;

